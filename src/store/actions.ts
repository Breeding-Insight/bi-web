import { ERROR_STATE } from './mutation-types';

export const actions = {
    // Example: Clears our login failed state after we have notified user
    //clearLoginFailed(context: any) {
    //    context.commit(ERROR_STATE, {'loginFailed': false});
    //}
}