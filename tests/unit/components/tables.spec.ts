describe('New table row hightlighting shows when new row is specified');

describe('New table row highlighting disappears on next action', () => {

  it('unhighlights on row detail form being show', () => {});

  it('unhighlights on new row being added', () => {});

  it('unhighlights on other row being removed', () => {});

});

describe('Table edit form is displayed when edit button is clicked');

describe('Input error is displayed appropriately when validation error occurs.');

// Submit, deactivate
describe('Events are properly emitted from table component', () => {

  it('Emits submit event with edited object on editing save', () => {});

  it('Emits remove event with when object is deactivated', () => {});

  it('Does not emit submit when edit form closes');
});

// Minimum field input props, table props, edit form props
describe('Table renders correctly with minimum properties specified.');

// Maximum field input props, table props, edit form props
describe('Table renders correctly with all properties specified.');

